export enum FieldTypes {
  textBox = 'textBox',
  number = 'number',
  // select = 'select',
  textArea = 'textArea',
  checkBox = 'checkBox',
  radio = 'radio',
  range = 'range',
  // table = 'table'
}
